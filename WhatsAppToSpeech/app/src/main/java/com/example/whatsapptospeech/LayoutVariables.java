package com.example.whatsapptospeech;

import android.util.Log;
import android.widget.Button;
import android.widget.Switch;
import android.widget.TextView;

public class LayoutVariables extends MainActivity{
    // Instance reference
/*    private static LayoutVariables instance;

    // Variables
    public TextView dispMsg;
    public TextView dispState;
    public TextView dispPackage;
    public Switch StartListening;
    public Button SendNotification;

    // Constructor
    public LayoutVariables(){
        StartListening = findViewById(R.id.wts_ListenToNotifications);
        dispState = findViewById(R.id.wts_Status);
        dispPackage = findViewById(R.id.wts_PackageName);
        dispMsg = findViewById(R.id.wts_MsgDisplay);
        SendNotification = (Button) findViewById(R.id.wts_CreateNotification);
    }



    public static LayoutVariables getInstance(){
        if(instance == null) {
            Log.d("Layout Variables", "Layout instance created");
            instance = new LayoutVariables();
            return instance;
        } else {
            return instance;
        }
    }*/
}
